Our storage maintenance controller at `/app/bin/scrub-window` is producing unsafe RAID scrub timers. Repair the JavaScript under `/app` so the documented CLI validates arbitrary inventories and policies, selects due arrays, and places them deterministically around existing reservations and blackouts while honoring dependency completion gaps, deadlines, global parallelism, controller exclusivity, instantaneous rack power, and rolling rack-energy budgets. `/app/docs/scheduling-contract.md` defines the input schema and exact reconciliation rules.

For every successful run, write canonical `plan.json` and exactly one `systemd/<escaped timer unit>.d/scrub-window.conf` for each due job beneath the requested absolute output directory; exclude reservations and non-due arrays, remove stale artifacts, and preserve deterministic ordering and timestamps. Invalid or infeasible input must exit nonzero without creating or changing the requested output. Keep the command offline and leave `/app/bin/scrub-window` running the repaired implementation.

Our archive host must accept only the encrypted backup cartridges reported by `/app/bin/cartridge-inventory`, but its rejected deployment trusts arbitrary removable media and can strand privileged mappings after failures. Reconcile the native policy under `/etc` against that inventory and `/app/docs/cartridge-policy.md`; do not change `/app`. Replace the legacy unit with the contract’s sysusers, tmpfiles, udev, `archive-cartridge@.service`, `archive-cartridge-quarantine@.service`, per-profile drop-ins, logrotate policy, and `/etc/systemd/system/archive-cartridge.slice`. Enable exactly the discovered profiles through symlinks at `/etc/systemd/system/multi-user.target.wants/archive-cartridge@<profile>.service` targeting `/etc/systemd/system/archive-cartridge@.service`. Sysusers declares `g archive-operators -`, `g archive-cartridge -`, and `u archive-cartridge - "Archive cartridge worker" /var/lib/archive-cartridge /usr/sbin/nologin`; tmpfiles creates the contract paths plus `/var/lib/archive-cartridge/quarantine` as `0750 archive-cartridge archive-operators`.

Emit one udev line per cartridge using, in order, `SUBSYSTEM=="block"`, `ENV{ID_FS_TYPE}=="crypto_LUKS"`, `ENV{ID_FS_UUID}=="<luksUuid>"`, `ENV{ID_SERIAL_SHORT}=="<deviceSerial>"`, `TAG+="systemd"`, and `ENV{SYSTEMD_WANTS}+="archive-cartridge@<profile>.service"`; expansions and wildcards are forbidden. The hardened `Type=exec` template and slice retain every namespace, filesystem, syscall, capability, logging, lifecycle, and aggregate resource control in the contract. The template sets `RequiresMountsFor=<source>` and the six environment assignments `PROFILE=%i`, `SOURCE=<source>`, `STATE_DIR=<stateDirectory>`, `MOUNT_ROOT=<mountRoot>`, `LOCK_FILE=<lock>`, and `WORKER=<worker>`; it also sets `StandardOutput=journal`, `StandardError=journal`, and `SyslogIdentifier=archive-cartridge-%i`. It sets `StartLimitIntervalSec=<failureWindowSeconds>`, `ConditionPathExists=!/var/lib/archive-cartridge/quarantine/%i.blocked`, `OnFailure=archive-cartridge-quarantine@%i.service`, and `OnFailureJobMode=replace-irreversibly`. Each drop-in binds the escaped UUID device; sets `DEVICE=/dev/disk/by-uuid/<luksUuid>`, `MAPPING=<mapping>`, `FILESYSTEM_UUID=<filesystemUuid>`, and `MOUNT_OPTIONS=<mountOptions joined by commas>`; uses `LoadCredential=luks.key:<credential>` rather than `LoadCredentialEncrypted`; joins the slice; and sets `RuntimeMaxSec`, `IOWeight`, `MemoryMax=<memoryMiB>M`, `LimitFSIZE`, and `StartLimitBurst=<failureBurst>`.

Keep the locked lifecycle’s exact command ordering: `flock -x 9`, `cryptsetup open --type luks` with `${CREDENTIALS_DIRECTORY}/luks.key`, `blkid -s UUID -o value`, mount, then `exec /usr/bin/setpriv --bounding-set=-all --inh-caps=-all --ambient-caps=-all --no-new-privs -- "${WORKER}"` with profile/source/destination/state arguments; follow it with `ExecStopPost=-/bin/umount -l ${MOUNT_ROOT}/${PROFILE}` and `ExecStopPost=-/usr/sbin/cryptsetup close ${MAPPING}`. The quarantine template is a root oneshot with `UMask=0027`, `NoNewPrivileges`, private tmp, strict system/home protection, only the quarantine directory writable, and exactly `CAP_CHOWN CAP_DAC_OVERRIDE CAP_FOWNER`; it runs `ExecStart=/usr/bin/install -m 0640 -o archive-cartridge -g archive-operators /dev/null /var/lib/archive-cartridge/quarantine/%i.blocked`. This marker affects only its profile. Preserve logrotate, embed no credentials, keep sysusers/tmpfiles reapplication safe, pass `systemd-analyze verify`, and make `/app/bin/cartridge-policy-check /` print `policy: ok`.

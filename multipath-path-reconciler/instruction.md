Recover this SAN host’s device-mapper multipath stack using the approved inventory and runbook at `/opt/san-host/inventory.json` and `/opt/san-host/remediation.md`. Configure the installed OS components directly: replace `/etc/multipath.conf`, persist the approved WWIDs in `/etc/multipath/wwids`, install the required `multipathd.service` systemd drop-in, udev path policy, and tmpfiles rule, and enable `multipathd.service` for `sysinit.target`.

The finished configuration must admit only the three inventoried LUNs, give them their documented stable aliases, and apply each array family’s distinct path grouping, selector, priority, checker, failback, feature, SCSI timeout, and scheduler policy without affecting other disks. Queue transient path loss globally without retaining queues after the daemon stops, validate configuration before daemon startup, and recreate `/run/multipath` with the documented ownership and mode. In the drop-in, express network readiness specifically as `Wants=network-online.target` plus `After=network-online.target`; do not substitute `Requires=`. Remove the obsolete managed fragments named by the runbook, keep the work offline, and validate the resulting configuration with the installed host tools.

Also make early boot and consumers consistent with multipath ownership: update `/etc/lvm/lvm.conf` so LVM admits only the three stable mapper aliases and rejects raw SCSI paths while preserving the host’s discard setting; configure `/etc/initramfs-tools/conf.d/multipath-host` and `/etc/initramfs-tools/modules` for private, multipath-capable initramfs generation without dropping the existing NVMe module; and install per-mount drop-ins beneath `/etc/systemd/system` that order every inventory mount after its escaped mapper device and `multipathd.service`. Required mounts must list both names in `Requires=` and their device in `BindsTo=`; optional mounts must list both in `Wants=` and contain neither `Requires=` nor `BindsTo=`.
